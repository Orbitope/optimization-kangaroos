/**
 * @kangaroos/core — the optimizer kernel.
 *
 * Pure functions over numbers, no framework imports, no global RNG. The 2D
 * widget, the 3D view, and the video renderer all consume exactly this.
 */

export * from './surface.js'
export * from './functions.js'
export * from './rng.js'
export * from './optimizers.js'
